# boatrace_saas を触るときの決まり

半自動運用のデータ分析マイクロSaaS。人の介在は1日5分を上限に設計している。
以下は実際に事故が起きた箇所と、その再発を防ぐための決まり。

## main（本体エージェント）のロール

**ユーザーが2026-09-09に提示した定義。ここは指定されたとおりを正とする。**
既存の記述に合わせて薄めないこと。

### SYSTEM ROLE

あなたは自己進化型意思決定システムである。目的は、与えられた問題を解決する
だけでなく、**自身の思考プロセスと出力精度を毎イテレーションで再帰的に改善する
こと**にある。

### OPERATIONAL ARCHITECTURE

すべてのタスク実行において、次の4フェーズを厳格に順守する。

1. **META-PLANNING（計画とプロンプト自己生成）**
   課題を分析し、現在の自身の知識・能力の限界を明確化する。
   そのうえで、このタスクを解くための最適化された内部サブプロンプトを生成する。
2. **EXECUTION（実行）**
   生成したサブプロンプトに従って解を出力する。
3. **CRITIQUE & ERROR TAXONOMY（自己批判とエラー分類）**
   自身の出力を客観的かつ論理的に検証する（忖度・妥協は禁止）。
   論理的整合性・事実正確性・冗長性・目的達成度の4観点に0〜100の点数を付け、
   発生したエラーやボトルネックの原因を特定・分類する。
4. **SELF-MUTATION（自己プロンプトの改変）**
   評価に基づき、次回同種タスクの最適化ルールを抽出し、Meta-Rules に追加更新する。

### 出力の作法

**4フェーズは内部で回す。フェーズごとの見出しを画面に出さない**
（2026-09-09、ユーザーの指定で改めた。当初は
`【思考プロセス / Sub-Prompt】【実行結果】【自己評価スコアと問題点分析】
【更新されたMeta-Rules】` の4見出しで出力する形だった）。

- ユーザーへの出力は**最低限**にする。求められた結果と、その判断に要る事実だけ。
- 自己評価スコアと反省を毎回並べない。**内部で採点し、外に出すのは
  その結果として変えた点だけ。**
- **本当にユーザーの判断が要るときだけ問う。** 自分で決められることを
  選択肢にして差し出さない。方針が割れる、後戻りできない、外向きに公開する、
  のいずれかに当たるときだけ確認する。
- 抽出した Meta-Rules は黙って下の節へ積む。積んだこと自体を報告しない。

### Meta-Rules（積み上げる自己改善指示）

ここに、フェーズ4で抽出したルールを追記していく。**消さずに積む。**
過去のルールと矛盾する新ルールを足すときは、古いほうを取り消し線ではなく
「いつ・なぜ置き換えたか」を書いて更新する。

1. **能力の境界を先に申告する。** 実行できない指示は代替手段を明示し、代替である
   ことを隠さない（自己プロンプトの改変は外部ファイルへの書き出しで代替している）。
2. **スコアと主張は検証可能な証拠に紐づける。** 事実正確性を主張するときは、テストの
   終了コード・実測値・一次ソースのいずれかを併記する。自己申告だけの高得点を出さない。
   2026-09-09、テストが終了コード1で落ちているのに「136件通過」と報告した失敗がある。
3. **フェーズ3では成功より失敗の列挙を優先する。** 忖度の禁止は「良い点も書く」の否定。
4. **Meta-Rules は積み上げ、置き換えるときは理由と日付を残す。** その場限りの反省に
   しない。
5. **形式を守ること自体を成果と勘違いしない。** フェーズは思考の質を上げるためにあり、
   見出しを並べるためではない。出力量はユーザーの負担であり、費用でもある。

## チーム内での担当範囲

上のロールとは別に、複数エージェントで進めるときのファイル所有を決めてある。
これはロールの一部ではなく、衝突を避けるための編成上の事実である。

- **main が書く**: `scraper/` `db/` `jobs.py` `pipeline.py` `backtest.py`
  `experiment.py` `tests/` `.github/`
- **main が独占する操作**: 公式サイトへのアクセス、Supabaseへの書き込み、
  モデルの採否、スキーマ変更、配備
- **main が持たない**: 配色・書体・尺度の方針（デザイナー、`DESIGN.md`）、
  公開画面の実装（web、`web/app/` `web/components/` `web/lib/`）、
  事業の筋・値付け・訴求（PM、読み取り専用）、変更の検証（reviewer、読み取り専用）

**ネットワークとDBを独占する理由。** `scraper/session.py` の2秒間隔はプロセス内で
しか効かない。収集を2本同時に走らせると、サイトから見た間隔は半分になる。DBも
同じで、書き手が複数いると `odds_snapshots` の重複防御が素通りする。

**コミットとプッシュは、ユーザーが指示したときだけ行う。**

## DBに書く経路を足すとき

**`db/schema.sql` でそのテーブルの書き込み方式を確認してから書くこと。**

| テーブル | 方式 |
|---|---|
| `odds_snapshots` | **追記専用の時系列**。同じ内容を二度入れると推移が壊れる |
| `races` `race_entries` `predictions` `race_results` | UPSERT。何度入れても同じ状態に収束する |

**取り込み関数は、同じ入力で何度呼んでも状態が変わらないこと。** `prerace-loop` は
各パスの直後に日次ファイル「全体」を取り込ませる設計なので、冪等でない書き込みを
足すと即座に重複が積み上がる。実際に1レースあたり13件のオッズが積まれる事故を起こした。
`_insert_odds` が直前と同じ内容を弾いているのはこのため。この防御を外さないこと。

## モデルを変えるとき

- 採否は `py -3 experiment.py` の**検証側**の対応のある比較でのみ判断する。
  差が標準誤差の2倍を超えなければ優劣は言えない。誤差に埋もれるなら単純なほうを採る。
- **的中率で判断しない。**「1号艇を常に選ぶ」だけで約50%出るうえ、
  予測の自信度が正しいかを何も語らない。
- 候補を複数試したときは分割位置を変えて符号と大きさが安定するか確認する。
  多重比較のぶん、一番良いものが偶然通ることがある。
- 配備するときだけ全データで係数を取り直す。判断は必ず分割で行う。
- **`CALIBRATED` は事実の記録であって、公開の門番ではない（2026-09-07 に変更）。**
  以前は「市場オッズを上回るまでEVを公開画面に出さない」という決まりで、
  RLSと `web/lib/data.ts` の両方で塞いでいた。AI予想を出す方針に変わったので
  門番は外してある。**性能が足りないことを理由に予測の表示を止めないこと。**
  改善は並行して進める課題として扱う。
- **ただし `CALIBRATED` を `True` に書き換えてはならない。** これは
  「検証で市場オッズを上回ったか」という事実を表す値で、`predictions.calibrated`
  に刻まれ、あとから成績を追うときの前提になる。公開したいから真にする、をやると
  過去の予測がどのモデルのものだったか分からなくなる。
- **画面に出す確率はモデル単独ではない。** `scraper/scoring.py` の
  `BLEND_WEIGHT`（現在0.2）で市場オッズへ引き戻した `pub_prob` を出している。
  モデル単独だと三連単の的中目で市場に +0.3833 ± 0.0428 負けており、推奨買い目の
  EVが軒並み2〜5倍になった。**この重みを上げるときは `py -3 experiment.py` の
  `trifecta_blend_check` を見ること。** 0.4以降は差が標準誤差の2倍を超える
  （2026-09-11 に更新。以前は「0.3以降」だった。三連単の展開に着順相関の補正を
  入れてモデル単独の負けが +0.3422 → +0.2104 に縮み、上限が一段上がったため。
  ただし 0.3 は標準誤差の1.9倍と境目で、学習側は相変わらず 0.00 を選ぶので
  **重み自体は 0.2 に据え置いてある**）。
  `model_prob` はモデル単独の出力として別に残してある（記録用、画面には出さない）。
- **三連単の展開は `scraper/order_corr.json` の補正表に依存している。**
  この表が消えると、`trifecta_probs` は黙って補正なしの掛け算に戻る。検証で
  標準誤差の6〜10倍の改善が出ている部分なので、消失は `tests/test_trifecta.py` の
  `DeployedTableTest` で失敗させてある。作り直しは `py -3 experiment.py fit-order`。
  **この表は全データの着順から作るので、検証に使ってはならない。** 検証側は
  学習側だけから作った表を `trifecta_probs(probs, corr)` で明示的に渡すこと。
  表の正規化は1着ごとに行う。全体で均すと1着の確率まで動き、画面に出している
  勝率と三連単が食い違う。
- **三連複は三連単を畳んだものであり、別のモデルではない。** `trio_probs` は
  `trifecta_probs` の120通りを着順ごとに20通りへ足し合わせているだけで、これは
  近似ではなく厳密な変換である。したがって三連複のために係数を取り直したり、
  別の重みを置いたりしないこと。検証（2026-09-16、1067レース）でも、配備中の
  `BLEND_WEIGHT` 0.2 は三連複で +0.0003 ± 0.0030 と市場並みだった。上限は
  三連単より緩く 0.4 まで誤差の範囲だが、**ゆるいほうに合わせて上げない。**
  同じ `model_prob` から両方を作っている以上、券種ごとに重みを変えると
  同じレースの同じ3艇に2つの確率が並ぶ。
- **三連複のオッズを三連単から計算して作らない。** 別勘定の投票で払戻も別に
  決まる。三連単オッズの逆数を6通り足せば「市場が着順を問わずその3艇と見て
  いる確率」は出るが、それは三連複の配当ではない。画面に出すオッズは実際に
  払い戻される数字でなければ意味がないので、`odds3f` を別に引いている。
  そのぶん1レースあたり1リクエスト増えており、**この増加は締切間際のレースの
  取りこぼしに直結する**（`prerace-loop` は締切の早い順に取るため）。
  これ以上券種を足すときは、巡回1周にかかる時間を先に測ること。
- **買い目の本数を券種で揃えない。** 三連単は120通りから5本、三連複は20通りから
  3本（`TRIFECTA_PICKS` / `TRIO_PICKS`）。三連複の5本は場の4分の1にあたり、
  三連単の5本（4%）と同じ見た目で並べると張る範囲の違いが隠れる。
  画面でも的中しやすさではなく期待値で比べさせること。
- **予想・期待値・推奨買い目を画面に出すときは、免責事項を必ず添える。**
  推定であって的中を保証しないこと、購入は自己責任であること、20歳未満は
  購入できないこと。レース詳細の予想エリアと `/about#disclaimer` の両方に置いてある。

## スケジュールを設計するとき

**GitHubのcronは1日2〜3回しか発火せず、1〜4時間ずれる。** 本数を増やしても
発火数は増えない（実測）。発火時刻の精度に依存する設計にしないこと。
ジョブは「いつ起動されても正しく動く」ように書く。`results` が実行時刻から
対象日を判定し、`prerace-loop` が終了時刻を過ぎていたら何もしないのはこのため。

### 朝のうちに全レースのオッズを揃える（`morning-odds`）

**その日の最も早い締切は毎日08:32である**（モーニング開催。2026-09-12〜19 の
8日間すべて）。「朝10時ごろが1R」ではない。スケジュールの話はこの08:32を
基準にすること。

`morning-odds` は、第1レースの締切 `MORNING_ODDS_MARGIN_MIN`（10）分前までに、
その日の全レースのオッズを揃えることを目標とするジョブ（2026-09-19、
ユーザーの指定）。締切間際に取る通常の巡回を置き換えるものではなく、その前に
「朝の一枚」を足す。オッズは追記専用の時系列なので、朝の一枚を入れても
締切前の一枚はそのまま入り、画面は常に最新を出す。

**この5つを緩めないこと。**

- **発売前のレースがあるのは正常。** オッズが無いことで落とさない
  （`require_odds=False`）。ここを厳しくすると毎朝180件の通知が鳴り、
  「通知が来なければ正常」という運用前提のほうが先に壊れる。
  **取得0件そのものでも落とさない**（2026-09-19 に改めた。当初は「落とすのは
  1レースも取れなかったときだけ」としていたが、オッズの発売が第1レースの
  締切より後に始まるなら毎朝それに当たるため）。理由と代わりの歯止めは
  後述の「0件が正常な場合と異常な場合を取り違えないこと」に書いた。
- **発売前に全レースを叩かない。** 先頭から `MORNING_ODDS_MISS_STREAK`（6）
  レース続けて取れなければその周を畳む。対象は締切の早い順なので、先頭が
  発売前なら後ろはもっと発売前である。畳まないと、何も返らないリクエストを
  毎周180回ぶん公式サイトへ送ることになる。
- **揃ったら即座に抜ける。** 揃った後も回り続けるとランナーを占有するだけ。
- **第1レースの締切を過ぎてから起動されたら何もしない**（2026-09-20 に追加）。
  cronは1〜4時間遅れるので開催中に発火しうる。そこから1周回すと、168レースで
  約76分（実測）を開催時間帯に使い、締切前の収集を担う `prerace-loop` を
  後ろへ待たせる（`concurrency` が直列化するので、同時アクセスではなく遅延と
  して出る）。締切を過ぎた時点でこの仕事の目的は達成できず、その日のオッズは
  `prerace-loop` が締切前に取る。手で `--until` を渡したときは止めない。
- **周の途中でDBへ取り込む**（`MORNING_ODDS_SYNC_EVERY`、2026-09-20 に追加）。
  **取れていても、DBに入るまでは画面に出ない。** 1周は168レースで約76分かかる
  ので、取り込みを周の終わりに1回だけ行うと、最初に取った締切の早いレースが
  画面へ出ないまま締切を迎える。実際、07:29に取った三国1R（締切08:32）がDBへ
  入ったのは08:45だった。取得が間に合っていても公開が間に合わなければ、
  この仕事の目的（第1レース前に買い目を出す）は果たせていない。

**発売開始は場ごとに違う（2026-09-20 実測）。** 08:00前後の時点で、第1レースが
15:19の住之江は12レースすべて取れた一方、第1レースが10:50の児島は2レースしか
取れなかった。「第1レースが早い場ほど早く発売」ではない。三連単と三連複でも
ずれがあり、三国は三連単11／三連複1、戸田は12／4だった（同じレースを11:09に
引き直すと4件とも全通り取れたので、パーサーではなく発売の時間差である）。
**したがって「朝に全レース揃う」は前提にできない。** 実測では168レース中
121レース（72%）で、残りは発売前だった。揃わないぶんは締切前の巡回が取る。

**cronではこの時刻を保証できない。** 実測7日連続で、09:00 JST 指定のcronは
毎日10:45〜11:00に発火している（遅れ30分〜3時間30分）。04:23という早い時刻・
奇数分に置いて余裕を取ってあるが、確約ではない。確実にするには外部から
`workflow_dispatch` を叩く必要がある。

`tests/test_jobs_guards.py` の `MorningSweepTargets`・`MorningSweepFailure`・
`MorningSweepFailureClassification` が固定している。

### 連鎖起動に手を入れるとき

`prerace-loop` は各実行の最後に次を `workflow_dispatch` で予約する。
**この連鎖には暴走の歯止めが3つ入っている。外さないこと。**
開催時間帯の外では予約しない、`results` からは予約しない、10分未満で
終わった実行からは予約しない。**開催時間帯の上限は定数で持たず、その日の
最終締切から決める**（`jobs.py last-close`、2026-09-20 に 21:40 の定数から
変更）。ナイターの最終レースは 21:40 より後に締切を迎えるためで、大村は
毎日 21:51 / 22:20 / 22:45 に締切がある。定数で切っていた間、22時台の
取得率は 0/6 だった。`last-close` は日次JSONから読むだけでネットワークに
出ない（予約のたびに開催場一覧を引くと1回15リクエストの無駄になる）。開催時間帯の下限は08:00（2026-09-19 に
09:00から下げた。最も早い締切が08:32で、`morning-odds` がその直前に
終わるため、09:00のままだと朝の一括取得から連鎖が始まらない）。3つ目が特に重要で、これが無いと、すぐ落ちる
状態のときに1分ごとの起動を繰り返して実行時間と公式サイトへの負荷を
際限なく使う。

**朝の一括取得は、そのまま巡回へ続けること。** `morning-odds` は第1レースの
締切前に終わるが、次のcronが発火するのは1〜4時間あとになる。2026-09-20、
一括取得が08:45に終わったあと10:52まで誰も収集せず、その間に締切を迎えた
9レースがオッズ不完全のまま残った。同じ実行の中で `prerace-loop` へ移り、
その終わりで次を予約させる。終了時刻は実行開始から5時間20分（1ジョブの上限
6時間に、一括取得に使った時間を含めて収めるため）。cronが遅れて締切後に
起動された日も、一括取得が即座に終わってここから巡回が始まるので、
連鎖の起点として働く。

予約の失敗（典型はPATの期限切れ）は必ずジョブの失敗にすること。
黙って成功にすると、その日の残りが収集されないまま気づけない。

### 締切に間に合わない取得は「取れた」ではなく劣化

**予測は締切前に入って初めて意味がある。** 締切を過ぎてから入ったものは、
画面に出ても誰も使えない。実測（2026-09-09）で、その日オッズが取れた90レースの
うち5レースが締切後、11レースが締切5分前だった。原因は2つあり、どちらも
歯止めを入れてある。外さないこと。

- **対象は締切の早い順に取る。** 場ごとの並びのままだと、2分後に締切のレースが
  順番待ちの最後尾に入る。1レースに数秒かかるので、順番が来る前に締切を過ぎる。
- **巡回間隔は窓の半分以下。** そうでないと締切前に一度しか見ないレースが出て、
  その一度が順番待ちで手遅れになる。粗い設定では `prerace-loop` が起動しない。
- **締切後に取得したレースは問題として数える。** 通知は失敗時にしか飛ばないので、
  ここで数えないとその日ずっと手遅れのまま気づけない。ただしループの1周目は
  除く（起動の遅れによる手遅れは、取り方の問題ではない）。

`tests/test_jobs_guards.py` が3つとも固定している。

### 何も取れなかったときは必ず失敗にすること

通知は失敗時にしか飛ばない。**「何も通知が来なければ正常」という運用前提は、
何も取れていない状態が必ず失敗になることに依存している。** ジョブを足すときは
「0件で正常終了する経路」を必ず潰すこと。

**0件が正常な場合と異常な場合を取り違えないこと。** 開催場が0場は異常
（ボートレースに開催0の日は無いので、パーサーが静かに空を返した証拠）。
一方「締切が近いレースが0」は時間帯によっては正常。両方 `jobs.py` で
区別してあり、`tests/test_jobs_guards.py` が固定している。

**「取れなかった」と「相手が答えなかった」も分ける（2026-09-19 に追加）。**
朝の一括取得（`morning-odds`）は、オッズが1レースも取れなくても失敗にしない。
発売開始時刻を実測していないため、発売前と取得失敗を区別できないからである。
区別できないまま疑わしきを失敗に倒すと、発売が第1レースの締切より後に始まる日は
毎朝通知が鳴り、本当の欠測が埋もれる。**オッズページが壊れたなら、同じ日の
`prerace-loop` が必ず失敗する。** 不具合の検知はそちらが担っているので、
ここでの0件は不具合の印ではなく「朝に取れる」という前提が外れている印になる。
前提が外れているなら、毎朝鳴らすのではなく cron から降ろすのが応え方である。
代わりに `first_odds_at`（初めてオッズが取れた時刻）と取得件数を日次JSONの
`morning_sweep` に残し、前提が成り立っているかを後から数字で判定できるように
してある。落とすのは `prerace-loop` と同じ2条件だけ（全パス失敗、通信エラーの
連続）。`tests/test_jobs_guards.py` の `MorningSweepFailureClassification` が
固定している。

ループの中では即座に落とさず、問題として集めて最後にまとめて落とす。
途中で落とすとその日の残り時間の収集がすべて失われるため。

**中止（順延）は欠測ではない。** 着順が永遠に入らないレースなので、取り直しの
対象から外し、印を残す。`scraper/result.py` の `parse_result` が中止を
`cancelled=True` の行として返し（取得失敗の `None` とは別物）、`jobs.py` が
日次JSONの行と `races.cancelled` に落とす。2026-09-09 の江戸川が全12レース
中止になり、着順の無いレースが12件残ったが、当時は中止も取得失敗も同じ `None`
だったため、欠測なのか中止なのかをDBから判定できなかった。
`tests/test_parsers.py` の `TestCancelledResult` と
`tests/test_jobs_guards.py` の `CancelledRaceIsNotMissing` が固定している。

**一過性の通信断を失敗として通知しないこと。** 公式サイトへの読み取りが15秒で
タイムアウトするのは日常的に起きる。以前は問題が1件でもあれば `prerace-loop` を
失敗にしていたので、20パス中2〜4パスが落ちただけの実行まで通知していた
（2026-09-09 のCI失敗2件はどちらもこれ）。**通知が日常化すると本当の欠測が
埋もれる。** いまは次の3つに限って落とす。

- 取り方の問題（締切後の取得・欠損）が1件でも出た … 相手側の都合ではないため
- 1パスも成功しなかった … その日のデータが丸ごと無い
- 通信エラーが `LOOP_OUTAGE_STREAK`（3）パス連続した … 一過性ではない

**この3つを緩めないこと。**「通知が来なければ正常」という運用前提は、この判定が
本当の劣化を必ず拾うことに依存している。`tests/test_jobs_guards.py` の
`LoopFailureClassification` が4条件とも固定している。

## 公式サイトへの負荷

- `scraper/session.py` の `SLEEP_SEC`（2秒間隔）を守る。
  **失敗した経路でも守ること。** `fetch` の待ちが `finally` にあるのは、
  サイトが5xxを返している間に呼び出し側が例外を握って進むと、全リクエストが
  失敗して待ち時間ゼロで連射になるため。相手が弱っているときに一番強く
  叩く形になる。`tests/test_defenses.py` が固定している。
- **同じ情報を二度取りに行かない。** `output/` をジョブ間でキャッシュしているのは、
  `prerace-loop` が `morning` の取った出走表を取り直さないようにするため。

## 秘密情報

- **接続文字列やキーをコマンドラインに書かない。** 会話ログに残る。
  `DATABASE_URL` は `C:\Users\ttuyu\supabase_dsn.txt` から読む。
  ```
  DATABASE_URL="$(py -3 -c 'import io;print(io.open(r"C:\Users\ttuyu\supabase_dsn.txt",encoding="utf-8-sig").read().strip(),end="")')" py -3 -m db.loader ...
  ```
- フロントに置いてよいのは anon キーだけ。`service_role` は絶対に置かない。

## シェル

- Bashツールもユーザーの `!` プロンプトも **Git Bash（POSIX）**。
  `set VAR=値` や `%VAR%` は使わない。PowerShellが要るときは PowerShell ツールを使う。
- **日本語を含む `.ps1` はBOM付きUTF-8で書く。** Windows PowerShell 5.1 は
  BOMの無いファイルをANSI（cp932）として読むので、Bashのヒアドキュメントで
  書いたUTF-8のスクリプトは中の日本語が化ける。構文は通るので気づきにくい。
  2026-09-20、実行ログから要点を抜くスクリプトがこれで一致せず、空の要約を
  吐いた（ログ本体は無事だった）。
- **ヒアドキュメント内の `\n` や行末の `\` が実際の改行に化けることがある。**
  Python文字列に改行を埋めるときは `chr(10)` を使うか `print()` を分ける。
  パッチを書いたら必ず `py -3 -m py_compile` で確認する。

## 数字を報告する前に

- **JOIN越しの集計は `count(distinct ...)`。** `count(*)` は結合後の行数を数える。
  1レースを13件のオッズと結合して「433レース」と誤読した事故がある。
- 件数は既知の上限と突き合わせる。**1日は最大24場×12レース。**
- 製品UIの場所や外部サービスの仕様を記憶で断言しない。
  公式ドキュメントか実画面で確認してから書く（Supabaseの接続文字列の場所を外した）。

## コミット前

```
py -3 -m unittest discover -s tests
py -3 -c "import yaml,io; yaml.safe_load(io.open('.github/workflows/collect.yml',encoding='utf-8').read())"
```

ワークフローを変えたらYAMLのパースを、Pythonを変えたらテストを通すこと。

pushすると `.github/workflows/test.yml` が同じ2つを走らせる（2026-09-20 に
追加）。**手元で通してから押す決まりは変えない。** CIは取りこぼしの受け皿で
あって、門は手元にある。それまではテストが `collect.yml` の収集直前でしか
走っておらず、壊れたコミットの最初の犠牲がその日の収集だった。

## 上位監査官への報告

このリポジトリは `D:/claude` 配下の1プロジェクトであり、Project ID は **`BR-SAAS`**。
親フォルダの上位エージェント（Cross-Project Risk & Dependency Auditor）が、
配下プロジェクトの構造的リスクを横断で見るための報告を吸い上げている。

**作業を区切るときは、次のファイルを更新すること。**

    ../.claude/audit/inbox/BR-SAAS.json

形式の定義は `../.claude/audit/README.md`。所感や要約を書く欄は意図的に無い。
**測っていない指標は `null` のままにする。** それらしい数字を入れてはならない。
上位側の乖離検知は「申告値」と「一次ソースからの観測値」の差で動いており、
捏造された数字はその検知を素通りさせる。`null` は欠落として正しく計上される。

観測値のほうは `py -3 ../.claude/audit/collect.py --project BR-SAAS` が
`git status` / `git log` / GitHub Actions / Issues / テストの終了コードから
自動で取る。こちらは手で書けないし、書き換えてはならない。

**この報告は「よく見せる」ためのものではない。** CIが落ちているなら
`externally_blocked` は `Yes` にする。未コミットが残っているなら残タスクに数える。
2026-09-09に、テストが終了コード1で落ちているのに「136件通過」と報告した失敗がある
（Meta-Rules 2）。同じことを構造として繰り返さないために、この報告経路がある。
